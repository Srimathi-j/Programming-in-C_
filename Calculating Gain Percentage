#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>



int main() {
    float a, b, c, t, g, p;

    scanf("%f", &a);
    scanf("%f", &b);
    scanf("%f", &c);

    t = a + b;
    g = c - t;
    p = (g / t) * 100;

    printf("%.2f\n", p);

    return 0;
}
